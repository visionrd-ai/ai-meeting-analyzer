# Perfect AI Meeting Analyzer - Py3.11 / Windows compatible

# Core Flask and SocketIO
flask>=3.0.0
flask-socketio>=5.5.0
python-dotenv>=1.0.1

# Numeric stack (Py3.11)
numpy>=1.26.4,<2.0           # Py3.11-compatible; avoids NumPy 2.0 breakages with numba/others
scipy>=1.11,<1.14            # SciPy versions that support Py3.11

# Audio Processing
soundcard>=0.4.2
sounddevice>=0.4.6
soundfile>=0.12.1
resampy>=0.4.3
librosa>=0.10.1
pydub>=0.25.1
audioread>=3.0.0
ffmpeg-python>=0.2.0
noisereduce>=3.0.2

# Mic / I/O (choose a single PyAudio; do NOT include 'portaudio' here)
pyaudio>=0.2.14              # Has wheels for cp311 on Windows
# pyaudio-fork                # REMOVE or keep only one to avoid conflicts

# VAD (Windows note below)
webrtcvad>=2.0.10

# Optional / often tricky on Windows: comment out if it gives trouble
aubio==0.4.9 ; platform_system != "Windows"

# Transcription Engines
faster-whisper>=0.10.0
assemblyai>=0.17.0
deepgram-sdk>=3.0.0          # (remove duplicate at file end)
openai>=1.0.0
tiktoken>=0.5.0

# AI and ML
torch>=2.0.0
torchaudio>=2.0.0
transformers>=4.30.0

# Performance / tooling
numba>=0.59.0                # Supports Py3.11 + NumPy 1.26
cython>=0.29.36
psutil>=5.9.0

# Dev / test
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.0.0
flake8>=6.0.0
